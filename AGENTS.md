# AGENTS.md — 项目强制性约束

> 本文件对本仓库中的所有代码变更具有**最高优先级约束力**。
> 任何 AI 助手、模型或开发者在本项目中工作时，必须逐条遵守以下规则。
> 实施蓝图见 `plan.md`；本文件与 `plan.md` 冲突时，**以本文件为准**。
> 若某项需求与本文件约束冲突，必须先向用户报告冲突并等待指示，禁止擅自绕过。

---

## 1. 运行时环境约束（不可协商）

1. **PHP 必须兼容 7.2+**。
   - 禁止使用 PHP 7.3+ 才存在的语法与函数，包括但不限于：箭头函数 `fn()`、null 合并赋值 `??=`、数组解构嵌套改进、`hrtime()`、`is_countable()` 之外的 7.3+ 新增函数、list() 引用赋值等。
   - 禁止尾随逗号函数调用（7.3+）、禁止灵活 heredoc/nowdoc 缩进（7.3+）。
   - 每个新增/修改的 PHP 文件完成后必须能通过 `php -l`（7.2 语法）检查。
2. **禁止使用 PHP Composer**。
   - 禁止出现 `composer.json`/`composer.lock`（根目录）、`vendor/autoload.php` 引用、依赖 Composer 自动加载的任何库。
3. **数据库：MySQL 5.7.44+，只用 PDO (pdo_mysql)**。
   - 禁止 `mysqli_*`、`mysql_*`；禁止字符串拼接 SQL，一律预处理语句绑定参数。
   - 所有 SQL 必须兼容 MySQL 5.7（禁止仅 8.0 支持的语法，如窗口函数、CTE、`utf8mb4_0900_*` 排序规则）。
   - 所有表名必须经 `DB::table()` 加前缀，禁止硬编码表名。

## 2. 架构约束

1. **单一入口 + 伪静态**：前台请求一律经根 `index.php` 路由分发；禁止新建可被浏览器直接访问的功能性 PHP 脚本（`install/`、`user/` 的入口文件除外）。
2. **URL 生成**：所有站内链接必须由 `Router::url()` 生成，禁止在模板/代码中手写站内路径（重写模式切换时必须不破坏链接）。
3. **目录职责**：内核代码只允许放 `core/`；模板只允许放 `themes/`；**主题自有的静态资源（脚本/样式/图片）必须随主题放在对应主题目录内（`themes/{主题}/`，可建子目录如 `js/`），经 `Theme::assetsUrl()` 引用，禁止放 `assets/`**；插件只允许放 `plugins/`（插件自有资源随插件目录）；第三方静态资源只允许放 `assets/vendor/`；内核跨场景共用脚本（前台与后台视图均引用的，如验证码/密码强度校验）放 `assets/front/`，后台资源放 `assets/admin/`；上传文件只允许放 `uploads/`。
4. **禁止引入框架**（Laravel/ThinkPHP/Symfony 等）与模板引擎库（Twig/Smarty 等），模板使用原生 PHP 语法。
5. **第三方 JS/CSS 必须本地化**：禁止任何运行时 CDN 引用（含字体、图片、统计脚本）；本地化资源必须连同其字体文件一并放入 `assets/vendor/`，并在 `README.md` 登记名称、版本、来源 URL。

## 3. 安全约束（每一行代码都必须满足）

1. **输出转义**：所有输出到 HTML 的变量必须经 `e()` 或等效 `htmlspecialchars()` 处理；富文本（Markdown 渲染结果）必须经服务端白名单过滤后才可输出。
2. **CSRF**：所有改变状态的表单与 AJAX 请求必须携带并校验 CSRF token。
3. **鉴权**：后台每个动作执行前必须调用 `Auth::check_cap()`；禁止仅用"隐藏菜单/按钮"代替服务端权限校验；权限唯一判定依据是 `users.role` 字段。
4. **密码与口令策略（等保二级，服务端强制，前端提示不作判定依据）**：
   - 只准使用 `password_hash(PASSWORD_DEFAULT)` / `password_verify()`；禁止 md5/sha1 存密码。
   - 注册、安装建管理员、后台建号、改密四个入口必须统一调用 `Auth::validate_password_strength()`：长度 8–64、四类字符（大写/小写/数字/特殊）至少三类、不得含用户名、不得命中弱口令黑名单。禁止在任何一个入口省略校验。
   - 改密必须验证原密码；敏感操作（改密、改邮箱/手机、导出日志）必须重新验证当前密码。
   - 日志与报错中禁止出现明文密码、明文验证码、AccessKeySecret、SMTP 授权码；日志中的邮箱/手机号必须脱敏。
5. **Session**：登录成功后必须 `session_regenerate_id(true)`；Session cookie 必须 `HttpOnly`，HTTPS 下必须加 `Secure`；会话无操作超时（默认 30 分钟）必须实现；登出必须 `session_destroy()` 并使 cookie 失效。
6. **上传**：上传文件必须用 fileinfo 校验真实 MIME，白名单（jpg/png/webp/gif），随机重命名；`uploads/` 必须配置禁止 PHP 执行的规则。
7. **敏感文件**：`config.php`、`core/` 下文件必须无法被 HTTP 直接访问（rewrite 规则 + 文件头部 `defined('APP_BOOT') or exit;` 双重防护）。
8. **频率限制与锁定（等保二级·登录失败处理）**：登录必须实现"同一账号连续失败 5 次锁定 10 分钟"（持久化到 `users.login_fail`/`locked_until`，禁止只用 Session 计数）+ IP 维度限流；注册、验证码发送/校验接口必须实现频率限制（60s 重发间隔、每日上限、错误尝试达上限作废——上限由发送插件经 `plugin_option($plugin,'max_attempts')` 管理，缺省 2、内核钳制 1-5，达到上限立即置 used=1，禁止缺省高于 5）。
9. **输入校验**：所有 GET/POST 参数必须经统一校验器（`input_int/input_email/input_phone/input_slug/input_enum/input_text`）处理，禁止在业务代码中直接使用 `$_GET`/`$_POST` 原值；SQL 中无法参数绑定的部分（排序、LIMIT、表名）必须走白名单或整型强转。
10. **错误信息**：`debug=0`（默认）时禁止向页面输出文件路径、SQL 语句、堆栈信息；统一 404/500 错误页。
11. **安全响应头**：内核必须统一输出 `X-Content-Type-Options: nosniff`、`X-Frame-Options: SAMEORIGIN`、`Referrer-Policy`；前台文章页输出受限 CSP（仅允许 self 本地化资源）。
12. **审计日志完整性**：logs 表**只增不改**——禁止为日志提供任何 UPDATE/DELETE/编辑接口（含 DB 封装层）；仅允许按留存期（默认 180 天、下限 180 天）到期自动清理与管理员导出；日志查询/导出本身必须写一条审计记录。
13. **客户端 IP 获取**：所有需要客户端 IP 的代码（审计日志、登录锁定、IP 限流、验证码频率限制）必须统一调用 `client_ip()`，禁止直接读取 `$_SERVER['REMOTE_ADDR']`；`client_ip()` 默认只返回 `REMOTE_ADDR`，仅当 `ip_header_enabled=1` 且配置了标头名时才从指定标头取值，且必须：XFF 逗号链取第一个 IP、`filter_var(..., FILTER_VALIDATE_IP)` 校验、非法或为空时回退 `REMOTE_ADDR`。**禁止无条件信任 `X-Forwarded-For` 等可伪造标头**；该功能默认必须关闭（`ip_header_enabled=0`）。

## 4. 功能红线

1. **角色体系**：公开注册只能产生 `role=user`；`editor`/`admin` 只能由管理员在后台授予。任何代码路径不得绕过该规则。
2. **审核开关**：文章审核、评论审核均为可选开关（存 options）；开启时仅管理员可审核发布；游客只能看到 `published` 状态的文章与评论。
3. **默认插件**：`smtp-mailer` 与 `aliyun-sms` 预装但**默认禁用**；二者必须实现为符合 `plan.md` §7 规范的标准插件（通过 Hook 接入，禁止在内核中硬编码其逻辑）；验证码发送插件必须经 `register_verify_provider()` 声明渠道能力，内核对验证码能力的探测与策略读取仅认声明，禁止硬编码插件名。
4. **阿里云短信**：必须用手写的纯 PHP RPC 签名客户端（HMAC-SHA1，仅依赖 cURL），禁止引入阿里云官方 SDK（其依赖 Composer/Guzzle）。
5. **审计日志**：所有非游客操作及登录成败、锁定/解锁、角色变更、验证码发送与核验必须经 `blog_log()` 记录（含 category 与 result）；插件日志必须走 `plugin_log()`；日志内容必须脱敏。
6. **编辑器**：后台编辑器固定使用本地化的 **Vditor**（当前版本 3.11.3，已按 `assets/vendor/README.md` 裁剪清单瘦身）；未经用户明确同意不得替换。升级 Vditor 版本时，**必须先按该裁剪清单对新 dist 重新执行裁剪**（移除 dev/类型文件、mathjax 与未使用的图表语法懒加载库），禁止将裁剪过的文件恢复为官方完整包后直接提交；升级流程与验证方法见 `assets/vendor/UPGRADE.md`。
7. **密码过期功能**：必须预留完整实现（`users.password_changed_at` + `pwd_expire_enabled`/`pwd_expire_days` 开关 + 登录后强制改密拦截），且**默认必须关闭**（`pwd_expire_enabled=0`）；禁止在未获用户明确要求时默认开启。密码历史功能（`pwd_history_count`）同为预留、默认关闭。
8. **安全默认值**：安装程序写入的安全项默认值必须与 plan.md §4 一致（锁定 5 次/10 分钟、会话超时 30 分钟、日志留存 180 天、自定义 IP 标头关闭 `ip_header_enabled=0`、debug=0）；禁止调低这些默认安全水位。

## 5. 代码风格约束

1. 文件编码 UTF-8 无 BOM；Unix 换行符（LF）；缩进 4 空格，禁止 Tab。
2. 命名：类 `PascalCase`，函数/方法 `snake_case`（模板助手）或 `camelCase`（类方法），常量 `UPPER_SNAKE`；与已有代码风格保持一致。
3. 每个类文件顶部写一行职责注释；公开 API 函数写简短 docblock。
4. 注释使用中文，说明"为什么"而非复述代码；禁止注释掉的死代码入库。
5. 禁止 `error_reporting(0)`、禁止用 `@` 抑制错误；错误必须被处理或记录。
6. 禁止在代码中硬编码密钥、密码、AccessKey；配置一律走 `config.php`（由安装程序生成，不入库）或 options 表。

## 6. 流程约束

1. 开始编码前先阅读 `plan.md`；按其中 §13 的里程碑顺序实施，禁止跳步导致依赖缺失。
2. 每个里程碑完成后：对相关 PHP 文件执行 `php -l` 检查，并在回复中如实报告结果；失败必须修复，禁止隐瞒。
3. **文档同步矩阵（功能变更必须连带更新对应文档，与代码同批交付）**：
   - 新增钩子、插件 API、自定义路由、存储机制 → `plugins/README.md`（钩子表/API 表）+ `plan.md` §7。
   - 新增主题注入点、模板上下文字段、模板约定 → `themes/README.md`（§3 上下文表、§5.1 注入点约定）。
   - 新增第三方静态资源 → `assets/vendor/README.md` 登记（名称/版本/来源 URL）+ 根 `README.md` 资源清单。
   - 新增数据表/字段 → 同步 `install/schema.php`（新装生效）并说明存量站升级路径（惰性迁移或手工 SQL），必要时更新 `plan.md` 相关章节。
   - 新增随包插件 → 插件自带 `README.md`（接入步骤/设计要点）+ `plugins/README.md` 登记或示例引用。
   - 新增用户可见功能（如置顶、新页面）→ 根 `README.md` 功能概览。
   - 安装程序、伪静态规则变更 → `README.md` + `plan.md` + 重写示例（`.htaccess` / `nginx.conf.example` / `bt-panel.rewrite.conf`）三处保持一致。
4. 完成回复中必须**逐项如实报告文档同步情况**（更新了哪些文档；确无需更新时说明理由），禁止只报代码改动不提文档。
5. 测试数据、调试输出（`var_dump`/`print_r`/`die` 调试残留）禁止留在最终代码中。

## 7. 等保二级合规约束（GB/T 22239-2019，本项目的硬性验收项）

以下各项为等保二级相关控制点在本项目的落地要求，任何代码变更不得削弱既有防护水位；如需变更必须先向用户说明影响并获确认：

1. **身份鉴别**：
   - 口令复杂度：8–64 位、四类字符至少三类、不含用户名、不命中弱口令黑名单——四个设密入口（安装/注册/后台建号/改密）必须全部服务端强制校验。
   - 定期更换：密码过期功能（`pwd_expire_enabled`）预留完整、默认关闭；开启后 90 天（可配 30–365）强制改密，拦截逻辑不得有可绕过的路径。
   - 登录失败处理：连续失败锁定 + IP 限流 + 全程审计，见 §3.8。
   - 传输防窃听：README 必须要求管理后台使用 HTTPS；代码在 HTTPS 下自动为 cookie 加 `Secure`。
2. **安全审计**：
   - 审计覆盖：登录成功/失败、登出、锁定/解锁、角色变更、增删改重要数据、配置变更、模板/插件操作、验证码发送与核验——成功与失败结果均须记录。
   - 审计记录必须包含：日期时间、操作者（user_id+role 快照）、事件类别与动作、结果（success/fail）、IP、UA、脱敏详情。
   - 审计记录保护：只增不改（§3.12）、留存默认 180 天且下限 180 天（《网络安全法》第 21 条不少于六个月）、日志访问自审计、README 提供定期备份指引。
3. **入侵防范/输入验证**：统一输入校验层 + PDO 预处理 + XSS 白名单过滤 + 错误信息不泄露（§3.9、§3.10）。
4. **剩余信息保护**：登出销毁会话、调试信息不出现在生产输出。
5. **数据保密性**：口令加盐哈希、日志脱敏、密钥不落库不入代码。
6. **合规自查**：每个涉及认证、日志、设置的里程碑完成后，必须对照本节逐条自查并在回复中报告结果。
